# העלאה לאוויר: Railway (או שירות דומה עם דיסק קבוע)

האתר צריך **דיסק קבוע** (volume) כי מסד הנתונים הוא קובץ SQLite ותמונות המוצרים נשמרות כקבצים.
שירותים כמו Railway, Render ו-Fly.io מאפשרים לחבר דיסק כזה. המדריך כתוב עבור Railway, ושאר השירותים דומים.

## לפני שמתחילים

1. **Git:** הפרויקט כבר מאותחל ב-Git, אבל עדיין אין commit ראשון. כדי לפרוס צריך commit ואז דחיפה למאגר פרטי ב-GitHub:
   ```
   git add -A
   git commit -m "first version"
   git branch -M main
   git remote add origin https://github.com/<שם-משתמש>/hashoval.git
   git push -u origin main
   ```
   קובצי הסודות (`.env`), מסד הנתונים המקומי, גיבויים ותמונות **לא** עולים ל-Git (הם ב-`.gitignore`).
2. **מאגר פרטי:** צור את המאגר ב-GitHub כ-**Private**.

## הקמה ב-Railway

1. היכנס ל-railway.app, "New Project" ← "Deploy from GitHub repo" ובחר את המאגר.
2. **דיסק קבוע:** בשירות, הוסף Volume וחבר אותו לנתיב `/data`.
3. **פקודות:**
   - Build command: `npm run build` (ברירת המחדל)
   - Start command: `npm run start:prod`
     (מעדכן את מבנה המסד אם השתנה, ואז מפעיל את האתר)
   - Health check path: `/api/health`
4. **משתני סביבה** (Variables). חובה:

   | משתנה | ערך |
   |---|---|
   | `NODE_ENV` | `production` (בדרך כלל מוגדר לבד) |
   | `DATABASE_URL` | `file:/data/hashoval.db` |
   | `UPLOAD_DIR` | `/data/uploads` |
   | `BACKUP_DIR` | `/data/backups` |
   | `ADMIN_PASSWORD` | סיסמה ארוכה, לפחות 12 תווים |
   | `SESSION_SECRET` | מחרוזת אקראית, לפחות 32 תווים |
   | `RESEND_API_KEY` | המפתח מ-Resend |
   | `EMAIL_FROM` | `hashoval <orders@hashoval.co.il>` |
   | `OWNER_EMAIL` | הכתובת שלך להתראות |

   בלי `ADMIN_PASSWORD` ו-`SESSION_SECRET` חזקים, ההתחברות לאדמין תיחסם בכוונה.

5. **אתר בדיקה סגור** (עד שיש ספק סליקה): הוסף
   - `PAYMENT_PROVIDER` = `mock` (דף תשלום מדומה, לא נגבה כסף)
   - `SITE_PASSWORD` = סיסמה. כל האתר יבקש אותה, כך שרק מי שאתה נותן לו יכנס.

   כשמתחברים לספק סליקה אמיתי מסירים את שני המשתנים האלה.

## דומיין

1. ב-Railway: Settings ← Domains ← Custom Domain ← `hashoval.co.il` (או `www.hashoval.co.il`). תקבל רשומת CNAME.
2. ב-DNS של הדומיין (אצל הרשם): הוסף את הרשומה. HTTPS מוגדר אוטומטית.
3. שים לב: רשומות המייל של Resend (SPF/DKIM) שכבר הוספת נשארות כמו שהן.

## אחרי העלאה: בדיקה

1. `https://<הדומיין>/api/health` מחזיר `{"ok":true}`.
2. התחבר ל-`/admin/login`, הוסף מוצר עם תמונה ובדוק שהתמונה מוצגת.
3. בצע קנייה מלאה כלקוח (עם `mock`), ובדוק מיילים.
4. **גיבוי:** בעמוד "גיבוי" באדמין הורד גיבוי, ושמור אותו מחוץ לשרת. את הדיסק עצמו כדאי לגבות גם.

## גיבוי אוטומטי

בשרת החי האתר מגבה את עצמו: כמה דקות אחרי כל הפעלה, ואחר כך בדיקה כל שעה, ואם עברה יממה מהגיבוי האחרון נוצר חדש ב-`BACKUP_DIR`
(מסד הנתונים, ותמונות המוצרים בתיקיית `uploads` לידו). נשמרים 30 הגיבויים האחרונים (`BACKUP_KEEP` משנה את המספר, `AUTO_BACKUP=off` מכבה).
בלוג של השרת מופיעה שורה `Automatic backup saved` בכל גיבוי.

גיבוי שיושב על אותו דיסק לא מגן מפני תקלה בדיסק עצמו, ולכן הורד גם עותק לעצמך מעמוד "גיבוי" באדמין באופן קבוע.
(לא משתמשים ב-Cron Job נפרד של Railway כי הדיסק מחובר לשירות אחד בלבד.)

## עדכון האתר

דוחפים שינוי ל-`main` ב-GitHub, והשירות בונה ומפעיל מחדש לבד. הנתונים והתמונות על הדיסק נשארים.
עדכון קצר של האתר גורם להפסקה של שניות עד דקה.

## הערות

- **מופע יחיד:** SQLite מתאים למופע אחד של האתר. אין להגדיל למספר מופעים במקביל.
- **כתובת IP:** ההגנות מפני ניסיונות מרובים מסתמכות על כותרת `X-Forwarded-For` שקובע השירות. ב-Railway זה מוגדר.
- **סליקה אמיתית:** בשלב הזה נוספים מתאם לספק והגדרת Webhook. הנתיב `/api/payments/*` כבר פטור מסיסמת האתר הפרטי.
