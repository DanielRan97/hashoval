# תכנית פיתוח - חנות דוגמיות בשמים

2026-09-19

## סקירה כללית ומטרת הפרויקט

שם האתר : hashoval
דומיין: hashoval.co.il

חנות אונליין למכירת דוגמיות (decants) של בשמי נישה יוקרתיים, בהתחלה מהמלאי הקיים של בעל העסק (כ-27 בשמים ממותגים כמו Parfums de Marly, Creed, Xerjoff, Dior, Byredo, MFK, Initio ועוד).

**עקרונות מפתח לפרויקט:**

- **התחלה קטנה ומבוקרת** — כל עסקה מאושרת ידנית ע"י בעל העסק לפני שהיא יוצאת לביצוע, כדי לשמור שליטה על המלאי בזמן אמת בשלב הראשוני.
- **תמחור אוטומטי** — המערכת מחשבת מחיר דוגמית (2/5/10 מ"ל) אוטומטית לפי מחיר הבקבוק המלא וגודלו, כך שבעל העסק רק מזין נתוני בסיס.
- **ניהול מלאי חי** — כל דוגמית שנמכרת מפחיתה מ"ל זמינים מהבקבוק במלאי.
- **עיצוב אלגנטי ולא עמוס** — אתר יוקרתי בעברית (RTL), פשוט לניווט, בלי סרבול.
- **תשלום** — בשלב זה אין חיבור לסליקה אמיתית. יש להטמיע נקודת חיבור (integration point) לספק סליקה ישראלי (Tranzila / PayPlus / Meshulam / Grow) ברגע שנפתח חשבון עסקי; עד אז ההזמנה עוברת לתהליך אישור ידני על ידי בעל העסק.

**מטרת ה-MVP:** להריץ בזמן קצר אתר עובד שמאפשר למכור את המלאי הקיים, לבדוק ביקוש אמיתי, וללמוד אילו בשמים/גדלים הכי מבוקשים — לפני שמשקיעים בהרחבת מלאי.

## מבנה האתר - רשימת עמודים

**צד לקוח (ציבורי):**

1. **דף בית** — מבחר/היילייטס, כמה מילים על הקונספט (דוגמיות מקוריות מבשמי נישה), עיצוב אלגנטי, תמונה גדולה + כותרת.
2. **קטלוג/חנות** — רשימת כל הבשמים הזמינים, סינון לפי מותג, כרטיס לכל בושם עם תמונה + שם + טווח מחירים.
3. **דף מוצר בודד** — תיאור הבושם, בחירת גודל דוגמית (2/5/10 מ"ל) עם מחיר מתעדכן, כפתור הוספה לסל.
4. **עגלת קניות** — רשימת פריטים, סה"כ, עלות משלוח (עם ציון "משלוח חינם מעל X ₪" אם רלוונטי).
5. **צ'קאאוט** — פרטי משלוח + לקוח, סיכום הזמנה. בשלב הנוכחי (ללא סליקה): הודעה שההזמנה נשלחה לאישור ופרטי תשלום יישלחו בנפרד; בעתיד: מסך תשלום דרך ספק הסליקה.
6. **דף "אודות"** קצר — טון אישי, אמינות, מקוריות המוצרים.

**צד אדמין (פרטי, מוגן בסיסמה):**

7. **לוח בקרה (Dashboard)** — סיכום: הזמנות ממתינות לאישור, הכנסות החודש, בשמים עם מלאי נמוך.
8. **ניהול מוצרים** — ראה סעיף ייעודי בהמשך.
9. **ניהול הזמנות** — ראה סעיף ייעודי בהמשך.

האתר צריך להישאר **פשוט** — לא לבנות עמודים או פיצ'רים שלא רשומים כאן בשלב ה-MVP (למשל: אין צורך במערכת חשבונות משתמש ללקוחות, אין צורך בבלוג, אין צורך בהמלצות AI).

## עמוד אדמין - ניהול מוצרים ומלאי

**רשימת מוצרים:** טבלה של כל הבשמים במלאי, עם עמודות: מותג, שם, מ"ל זמינים כרגע, ערך שוק לבקבוק מלא, סטטוס (פעיל/אזל).

**הוספת/עריכת מוצר** — טופס עם השדות הבאים:

- מותג + שם הבושם
- תמונה (העלאה)
- תיאור קצר (2-3 שורות)
- גודל בקבוק מקורי (מ"ל)
- אחוז מילוי נוכחי (%) — מתעדכן אוטומטית עם כל מכירה, אך ניתן לתקן ידנית (למשל אחרי ספירת מלאי)
- ערך שוק משוער לבקבוק מלא (₪) — השדה שקובע את כל התמחור
- האם המוצר פעיל בחנות (checkbox)

**תמחור אוטומטי (read-only, מוצג למנהל):** המערכת מציגה לצד כל מוצר את שלושת מחירי הדוגמית המחושבים (2/5/10 מ"ל) לפי לוגיקת התמחור (ראה סעיף ייעודי). אין להזין אותם ידנית — הם נגזרים.

**התראת מלאי נמוך:** כאשר מ"ל זמינים בבושם יורד מתחת לסף (למשל 10 מ"ל), מוצג סימון/צבע אזהרה ברשימה.

**היגיון עדכון מלאי:** כאשר הזמנה מאושרת (ראה סעיף הבא), המ"ל הזמינים של המוצר הרלוונטי פוחתים אוטומטית לפי גודל הדוגמית שנמכרה. אם המלאי אוזל באמצע — לא לאפשר אישור הזמנה שחורגת מהכמות הזמינה.

## עמוד אדמין - ניהול הזמנות ואישור עסקאות

**תור הזמנות ממתינות לאישור** — כל הזמנה חדשה מהאתר נכנסת לכאן קודם, לפני שהיא זזה הלאה. לכל הזמנה מוצג: פרטי לקוח (שם, טלפון, כתובת), רשימת הפריטים שהוזמנו (בושם + גודל + מחיר), סה"כ לתשלום, עלות משלוח, תאריך.

**פעולות זמינות למנהל על כל הזמנה:**

- **אישור** — ההזמנה עוברת לסטטוס "מאושרת", המלאי מתעדכן (מ"ל פוחתים מהמוצרים הרלוונטיים), ונשלחת (בעתיד: אוטומטית, כרגע: ידנית) בקשת תשלום ללקוח.
- **דחייה** — עם שדה חופשי לסיבה (למשל: מלאי לא מספיק, חשד להזמנת סרק). ההזמנה מסומנת "נדחתה" ולא משפיעה על מלאי.
- **סימון כשולם** — לאחר שהלקוח שילם בפועל (כרגע ידנית - ביט/העברה, בעתיד אוטומטי דרך ספק הסליקה).
- **סימון כנשלח** — עם שדה חופשי להזנת מספר מעקב (tracking number) של חברת השילוח.

**סטטוסים אפשריים להזמנה:** ממתינה לאישור → מאושרת (טרם שולם) → שולם → נשלח → הושלם. או: ממתינה לאישור → נדחתה.

**היסטוריית הזמנות** — טבלה/רשימה של כל ההזמנות (לא רק הממתינות), עם סינון לפי סטטוס ותאריך, לצורך מעקב ודוחות.

## מודל נתונים - מוצרים, הזמנות, מלאי

**Product (מוצר):**
```
id, brand, name, description, imageUrl,
bottleSizeMl, currentFillPercent, marketValuePerBottle,
isActive, createdAt, updatedAt
```

**Order (הזמנה):**
```
id, customerName, customerPhone, customerAddress,
items: [{ productId, decantSizeMl, unitPrice, quantity }],
shippingCost, totalAmount,
status: pending_approval | approved | rejected | paid | shipped | completed,
rejectionReason, trackingNumber,
createdAt, updatedAt
```

**Assumptions/Settings (הגדרות גלובליות - נערכות באדמין):**
```
spillagePercent (ברירת מחדל 5%),
packagingCostPerUnit (₪),
paymentFeePercent,
multiplier2ml, multiplier5ml, multiplier10ml,
freeShippingThreshold (₪), standardShippingCost (₪),
lowStockThresholdMl
```

הערה חשובה: כאשר הזמנה עוברת לסטטוס "approved", יש לבצע את עדכון המלאי (הפחתת מ"ל) כפעולה אטומית יחד עם שינוי הסטטוס, כדי למנוע מצב של אישור כפול שמוריד מלאי פעמיים בטעות.

## לוגיקת תמחור אוטומטית לפי מ"ל

לכל דוגמית מחושב מחיר לפי הנוסחה:

```
מחיר בסיס למ"ל = ערך שוק לבקבוק מלא / גודל בקבוק (מ"ל)

מחיר דוגמית 2 מ"ל = מחיר בסיס למ"ל × 2 × multiplier2ml (ברירת מחדל 1.75)
מחיר דוגמית 5 מ"ל = מחיר בסיס למ"ל × 5 × multiplier5ml (ברירת מחדל 1.45)
מחיר דוגמית 10 מ"ל = מחיר בסיס למ"ל × 10 × multiplier10ml (ברירת מחדל 1.25)
```

המקדמים (multipliers) גדולים יותר לגדלים קטנים יותר, כי דוגמית קטנה "עולה" יחסית יותר למוכר (עלויות אריזה, איבוד בתהליך המילוי). המקדמים נערכים דרך הגדרות גלובליות באדמין ולא קבועים בקוד.

**חישוב מלאי זמין למכירה בפועל:**
```
מ"ל זמינים = גודל בקבוק × אחוז מילוי נוכחי
מ"ל נטו למכירה = מ"ל זמינים × (1 - spillagePercent)
```

כאשר בודקים אם יש מספיק מלאי לביצוע הזמנה, יש לבדוק מול "מ"ל נטו למכירה", לא מול "מ"ל זמינים" הגולמי.

## עיצוב וסטייל - אלגנטי, יוקרתי, עברית RTL

**כיוון עיצובי:** מינימליסטי ויוקרתי — הרבה שטח לבן/רווח, טיפוגרפיה עדינה, לא צבעוני ולא עמוס. השראה מאתרי בשמי נישה בינלאומיים (Byredo, Le Labo) שמדגישים איכות ופשטות על פני עומס גרפי.

**עקרונות קונקרטיים:**

- **פלטת צבעים מצומצמת** — 2-3 צבעים בלבד: רקע בהיר/שמנת או שחור עמוק, טקסט כהה, צבע הדגשה אחד (זהב עמוק, בורדו, או שחור-פחם) לכפתורים ולינקים.
- **טיפוגרפיה** — פונט serif עדין לכותרות (מקנה תחושת יוקרה), פונט sans-serif נקי לטקסט רץ. חובה תמיכה מלאה בעברית (לא רק תעתיק) עם RTL תקין בכל הדפים, כולל טפסים וטבלאות.
- **תמונות** — תמונות מוצר איכותיות על רקע אחיד (לבן/שחור), בלי רעש חזותי מסביב.
- **ניווט פשוט** — תפריט עליון מצומצם (בית, חנות, אודות, סל), בלי תת-תפריטים מסובכים.
- **לא להעמיס אנימציות, פופ-אפים, או באנרים** — כל אלמנט על המסך צריך להצדיק את מקומו.

**RTL טכני:** `dir="rtl"` ברמת ה-HTML, שימוש ב-CSS logical properties (`margin-inline-start` ולא `margin-left` וכו') כדי שהעימוד יתהפך נכון, ובדיקה ספציפית שטפסים, טבלאות ואייקונים (חצים, כפתורי "הבא/הקודם") מתהפכים בהתאם לכיוון.

## תהליך הזמנה, אישור ותשלום (כולל נקודת חיבור לסליקה)

**זרימת ה-MVP הנוכחית (ללא סליקה אמיתית):**

1. לקוח בוחר מוצרים, ממלא פרטי משלוח, שולח הזמנה → סטטוס "ממתינה לאישור".
2. בעל העסק רואה את ההזמנה באדמין, בודק מול המלאי בפועל, **מאשר** → מלאי מתעדכן, סטטוס "מאושרת".
3. בעל העסק שולח ללקוח בקשת תשלום ידנית (ביט עסקי / העברה בנקאית) מחוץ למערכת, לפי הסכום שמוצג בהזמנה.
4. כשהתשלום מתקבל בפועל, בעל העסק מסמן ידנית "שולם" באדמין.
5. משלוח מאורגן מול חברת השילוח, מספר מעקב מוזן, סטטוס "נשלח".

**נקודת החיבור העתידית לסליקה אמיתית** (ברגע שנפתח חשבון עסקי אצל ספק כמו Tranzila/PayPlus/Meshulam/Grow):

- שלב 3-4 מוחלף בקריאת API לספק הסליקה בעת האישור (או מיד בשליחת ההזמנה, תלוי בהחלטה עסקית) — הספק מחזיר קישור לדף תשלום מאובטח או מבצע חיוב ישיר.
- יש לתכנן את מבנה הקוד כך שפונקציית "עיבוד תשלום" תהיה מבודדת (interface/adapter) כדי שהחלפת הזרימה הידנית באוטומטית לא תדרוש שינוי במודל הנתונים או בשאר האתר — רק מימוש חדש של אותה פונקציה.
- **החלטה עסקית לתכנן מראש:** האם התשלום ייגבה **לפני** אישור המנהל (כמו רוב האתרים) או **אחרי** (כפי שהתהליך הידני עובד כרגע)? גבייה לפני אישור מהירה יותר ללקוח אך דורשת מנגנון החזר כספי (refund) למקרה של דחיית הזמנה; גבייה אחרי אישור שומרת שליטה מלאה אך מאיטה את התהליך. מומלץ להתחיל בגבייה אחרי אישור (תואם את התהליך הידני הקיים) ולעבור לגבייה מיידית בהמשך אם קצב ההזמנות יגדל.
אני רוצה שהלקוח יזמין ולא ידרג מכלום, מבחינתו זה ניראה כאילו הוא הזמין
אבל אני צריך לאשר את ההזמנה
במידה ואין לי יותר את הבושם ולא עידכנתי בדף המלאי שהמוצר אזל אז יש לי אפשרות לעשות בלחיצת כפתור ביטול הזמנה
הלקוח מקבל החזר כספי ומייל עם הסבר

## משלוחים ותמחור משלוח

- עלות משלוח קבועה (`standardShippingCost`) מתווספת לכל הזמנה כברירת מחדל.
- **משלוח חינם מעל סף** (`freeShippingThreshold`) — כאשר סכום ההזמנה עובר את הסף, עלות המשלוח מתאפסת אוטומטית בעגלה. זה מעודד הזמנות גדולות יותר בצורה טבעית (עדיף על תוספת תשלום קבועה שמרגישה כמו קנס).
- שדה הזנת **מספר מעקב** בהזמנה לאחר המשלוח, עם קישור (אם קיים) לדף המעקב של חברת השילוח.
- חברת השילוח עצמה (Baldar / Run / דואר ישראל / ציטה וכו') היא הסכם עסקי חיצוני ולא חלק מהקוד — בשלב זה מספיק שדה טקסט חופשי למספר מעקב; אינטגרציה אוטומטית (API של חברת שילוח) היא שיפור עתידי, לא MVP.

## שלבי פיתוח מומלצים ל-Claude Code

**שלב 1 — מודל נתונים + אדמין בסיסי**
בנה את מבני הנתונים (Product, Order, Settings), ואת עמוד ניהול המוצרים באדמין עם התמחור האוטומטי. בדוק שהנוסחאות נכונות עם 2-3 מוצרי דוגמה לפני שממשיכים.

**שלב 2 — חנות ציבורית (קריאה בלבד)**
דף בית, קטלוג, דף מוצר עם בחירת גודל דוגמית. עדיין בלי עגלה/צ'קאאוט — רק תצוגה.

**שלב 3 — עגלה, צ'קאאוט, יצירת הזמנה**
עגלת קניות, טופס פרטי משלוח, יצירת הזמנה בסטטוס "ממתינה לאישור". כולל לוגיקת משלוח חינם מעל סף.

**שלב 4 — אדמין: ניהול הזמנות ואישור**
תור ההזמנות, פעולות אישור/דחייה עם עדכון מלאי אטומי, סימון תשלום/משלוח.

**שלב 5 — עיצוב ולוטש**
רק אחרי שהפונקציונליות עובדת מקצה לקצה — להשקיע בעיצוב היוקרתי, RTL מדויק, רספונסיביות למובייל (רוב הלקוחות יגלשו מהטלפון).

**שלב 6 (בהמשך, לא ב-MVP) — חיבור סליקה אמיתית**
רק אחרי שיש חשבון עסקי אצל ספק סליקה, ולפי ה-adapter שתוכנן בשלב 1.

**המלצה כללית:** לבדוק כל שלב בפועל (לא רק לקרוא קוד) לפני מעבר לשלב הבא — במיוחד את חישובי התמחור והמלאי, כי טעות שם משפיעה על כסף אמיתי.
